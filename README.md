
  # Web3 PSP Admin (real))

  This is a code bundle for Web3 PSP Admin (real)). The original project is available at https://www.figma.com/design/e6yyLeiu8GubVuEfElszXz/Web3-PSP-Admin--real--.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  