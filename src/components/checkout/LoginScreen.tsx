import React from "react";

interface LoginScreenProps {
  // Props will be defined when we extract this screen
}

export const LoginScreen: React.FC<LoginScreenProps> = (props) => {
  return (
    <div>
      {/* Login Screen - To be implemented */}
    </div>
  );
};
